;(function(){
    angular
        .module("app")
        .filter("chartSort", function(){
            let listeners = parseInt(artist.listeners); 
            let listenings = parseInt(artist.listenings); 
        }); 
})();